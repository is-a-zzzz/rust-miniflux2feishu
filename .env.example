# ================================
# Miniflux Webhook 配置文件
# ================================

# 飞书机器人 Webhook URL (必填)
# 获取方式：飞书群 -> 群设置 -> 群机器人 -> 添加机器人 -> 自定义机器人
WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-url

# Miniflux 服务器地址 (可选)
# 用于在飞书消息中生成 Miniflux 文章链接
# 示例：https://miniflux.example.com
MINIFLUX_URL=

# ================================
# 以下配置通常不需要修改
# ================================

# 日志级别 (可选，默认: info)
# 可选值：trace, debug, info, warn, error
# RUST_LOG=info

# 监听 IP (可选，默认: 0.0.0.0)
# IP=0.0.0.0

# 监听端口 (可选，默认: 8083)
# PORT=8083
