# 飞书 Webhook URL (必填)
WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/your-webhook-url

# 日志级别 (可选，默认: info)
# RUST_LOG=info

# 监听 IP (可选，默认: 0.0.0.0)
# IP=0.0.0.0

# 监听端口 (可选，默认: 8083)
# PORT=8083
